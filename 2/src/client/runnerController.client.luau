local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ContextActionService = game:GetService("ContextActionService")

local player = Players.LocalPlayer
local advanceEvent = ReplicatedStorage:WaitForChild("RunnerAdvance")

local moveSound = nil

local rainbowUIEvent = ReplicatedStorage:WaitForChild("RainbowGateUIEvent")
local TweenService = game:GetService("TweenService")

local laneEvent = ReplicatedStorage:FindFirstChild("LaneSwitch")
if not laneEvent then
	laneEvent = Instance.new("RemoteEvent")
	laneEvent.Name = "LaneSwitch"
	laneEvent.Parent = ReplicatedStorage
end


player.CharacterAdded:Connect(function(character)
    local humanoid = character:WaitForChild("Humanoid")

    humanoid.Died:Connect(function()
        -- Clone the sound from ReplicatedStorage
        local deathSound = ReplicatedStorage:WaitForChild("Kill"):Clone()
        deathSound.Parent = workspace -- parent somewhere in the world
        deathSound:Play()

        -- Clean up after it finishes
        deathSound.Ended:Connect(function()
            deathSound:Destroy()
        end)
    end)
end)

local jumpEvent = ReplicatedStorage:FindFirstChild("PlayerJump")
if not jumpEvent then
	jumpEvent = Instance.new("RemoteEvent")
	jumpEvent.Name = "PlayerJump"
	jumpEvent.Parent = ReplicatedStorage
end

local FORWARD_SPEED = 22

local LANE_X = { -10, 0, 10 }
local laneIndex = 2

local SEGMENT_LENGTH = 50
local TRIGGER_MULT = 0.7
local nextTriggerZ = -(SEGMENT_LENGTH * TRIGGER_MULT)

local function clampLane(i)
	return math.clamp(i, 1, #LANE_X)
end

local function blockMovement()
	local function sink()
		return Enum.ContextActionResult.Sink
	end
	ContextActionService:BindAction("BlockW", sink, false, Enum.KeyCode.W)
	ContextActionService:BindAction("BlockS", sink, false, Enum.KeyCode.S)
	ContextActionService:BindAction("BlockUp", sink, false, Enum.KeyCode.Up)
	ContextActionService:BindAction("BlockDown", sink, false, Enum.KeyCode.Down)
end

local function getRootPart(char)
	if not char then return nil end
	-- Try HumanoidRootPart first
	local hrp = char:FindFirstChild("HumanoidRootPart")
	if hrp then return hrp end
	-- Try PrimaryPart
	if char.PrimaryPart then return char.PrimaryPart end
	-- Find any anchored or main part
	local hum = char:FindFirstChildOfClass("Humanoid")
	if hum and hum.RootPart then return hum.RootPart end
	-- Last resort: find any BasePart
	return char:FindFirstChildWhichIsA("BasePart")
end

player.CharacterAdded:Connect(function(char)
	task.wait(0.5)
	blockMovement()

	local rootPart = getRootPart(char)
	if rootPart then
		nextTriggerZ = rootPart.Position.Z - (SEGMENT_LENGTH * TRIGGER_MULT)
	end

	-- Create sound for this character
	moveSound = Instance.new("Sound")
	moveSound.SoundId = "rbxassetid://139421450430380"
	moveSound.Volume = 1.0
	moveSound.Parent = player:WaitForChild("PlayerGui")

	-- Reset lane
	laneIndex = 2
end)

UIS.InputBegan:Connect(function(input, gpe)
	if gpe then return end

	if input.KeyCode == Enum.KeyCode.A or input.KeyCode == Enum.KeyCode.Left then
		laneIndex = clampLane(laneIndex - 1)
		laneEvent:FireServer(laneIndex)
		if moveSound then moveSound:Play() end

	elseif input.KeyCode == Enum.KeyCode.D or input.KeyCode == Enum.KeyCode.Right then
		laneIndex = clampLane(laneIndex + 1)
		laneEvent:FireServer(laneIndex)
		if moveSound then moveSound:Play() end

	elseif input.KeyCode == Enum.KeyCode.Space then
		jumpEvent:FireServer()
	end
end)

RunService.Heartbeat:Connect(function(dt)
	local char = player.Character
	if not char then return end
	local rootPart = getRootPart(char)
	if not rootPart then return end

	-- Segment trigger
	if rootPart.Position.Z <= nextTriggerZ then
		advanceEvent:FireServer()
		nextTriggerZ -= SEGMENT_LENGTH
	end
end)
-- Cupcake eat sound listener
task.spawn(function()
	local eatSoundEvent = ReplicatedStorage:WaitForChild("eat_drycereal")

	eatSoundEvent.OnClientEvent:Connect(function()
		local soundTemplate = ReplicatedStorage:WaitForChild("EatSound")
		local sound = soundTemplate:Clone()

		local gui = player:FindFirstChild("PlayerGui")
		if not gui then return end

		sound.Parent = gui
		sound:Play()

		sound.Ended:Connect(function()
			sound:Destroy()
		end)
	end)
end)

local function animateRainbowLabel(label)
    -- Initial state: invisible, center screen, big size
    label.AnchorPoint = Vector2.new(0.5, 0.5)
    label.Position = UDim2.new(0.5, 0, 0.5, -150) -- start above center
    label.TextTransparency = 1
    label.TextScaled = true
    label.Visible = true
    label.Size = UDim2.new(0, 500, 0, 150) -- bigger

    -- Tween 1: slide down + fade in + scale up (pop)
    local tweenInfo1 = TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
    local goal1 = {
        Position = UDim2.new(0.5, 0, 0.5, 0), -- center of screen
        TextTransparency = 0,
        Size = UDim2.new(0, 600, 0, 180) -- grows slightly for emphasis
    }
    local tween1 = TweenService:Create(label, tweenInfo1, goal1)
    tween1:Play()

    -- Tween 2: tiny wiggle left/right
    tween1.Completed:Connect(function()
        local originalPos = label.Position
        for i = 1, 3 do
            local leftTween = TweenService:Create(label, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {Position = originalPos + UDim2.new(0, -15, 0, 0)})
            local rightTween = TweenService:Create(label, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {Position = originalPos + UDim2.new(0, 15, 0, 0)})
            local centerTween = TweenService:Create(label, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {Position = originalPos})

            leftTween:Play()
            leftTween.Completed:Wait()
            rightTween:Play()
            rightTween.Completed:Wait()
            centerTween:Play()
            centerTween.Completed:Wait()
        end
    end)

    -- After 2 seconds, tween out: slide up + fade + shrink
    task.delay(2, function()
        local tweenInfoOut = TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.In)
        local goalOut = {
            Position = UDim2.new(0.5, 0, 0.5, -150), -- slide back above center
            TextTransparency = 1,
            Size = UDim2.new(0, 500, 0, 150) -- shrink back to initial
        }
        local tweenOut = TweenService:Create(label, tweenInfoOut, goalOut)
        tweenOut:Play()

        tweenOut.Completed:Connect(function()
            label.Visible = false
        end)
    end)
end
rainbowUIEvent.OnClientEvent:Connect(function()
	local gui = player:WaitForChild("PlayerGui")
	local rainbowUI = gui:WaitForChild("RainbowUI")
	local label = rainbowUI:WaitForChild("RainbowLabel")

	animateRainbowLabel(label)
	-- optional: auto hide after 2 seconds
	local soundTemplate = ReplicatedStorage:WaitForChild("RainbowSound") -- make sure you add a Sound in ReplicatedStorage
	local sound = soundTemplate:Clone()
	sound.Parent = gui
	sound:Play()

	-- Clean up after it finishes
	sound.Ended:Connect(function()
		sound:Destroy()
	end)
end)